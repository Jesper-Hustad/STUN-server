

// // eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
// const reverse = (object : Record<number, string>) => Object.entries(object).map((k, v) => [v, k])
//
// export { reverse }
